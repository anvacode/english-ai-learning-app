import 'dart:ui';

// Color palette for consistent theming across devices
class BadgeColors {
  // Unlocked badge colors
  static const unlockedPrimary = Color(0xFF4CAF50);
  static const unlockedLight = Color(0xFFC8E6C9);
  static const unlockedText = Color(0xFFFFFFFF);
  
  // Locked badge colors
  static const lockedDefault = Color(0xFFE0E0E0);
  static const lockedBorder = Color(0xFFBDBDBD);
  static const lockedText = Color(0xFF9E9E9E);
}
